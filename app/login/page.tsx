import LoginForm from '@/components/auth/login-form';

export const metadata = {
  title: 'Login - Chandanpur High School',
  description: 'Login to your school portal',
};

export default function LoginPage() {
  return <LoginForm />;
}
